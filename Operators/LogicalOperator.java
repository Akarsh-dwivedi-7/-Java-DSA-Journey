package Operators;

public class LogicalOperator {
    public static void main(String[] args) {
        // AND Operator(&&)
        // System.out.println((3 > 2) && (4 > 3)); // True
        // System.out.println((3 < 2) && (4 > 3)); // False
        // System.out.println((3 > 2) && (4 < 3)); // false
        // System.out.println((3 < 2) && (4 < 3)); // false

        // OR Operator(||)
        // System.out.println((3 > 2) || (4 > 3)); // True
        // System.out.println((3 < 2) || (4 > 3)); // true
        // System.out.println((3 > 2) || (4 < 3)); // true
        // System.out.println((3 < 2) || (4 < 3)); // false

        // NOT Operator(!)
        // System.out.println(!(5 > 3));
        // System.out.println(!(5 < 3));
    }
}
